node_modules/
.next/
out/
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*.local

# Design-tool output. The brand kit is generated elsewhere and re-exported whole;
# only the handful of assets the app actually serves live in the repo, under
# public/brand and app/. Keeping the full dump out avoids re-committing 600 KB
# every time one icon is regenerated.
exports/

# No sampled audio ever ships — see CLAUDE.md invariant 2.
*.wav
*.mp3
*.ogg
*.flac
*.aif
*.aiff
